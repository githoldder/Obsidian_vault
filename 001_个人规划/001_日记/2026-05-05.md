### 一、先明确你的服务器核心定位
 
你的腾讯云Lighthouse是唯一国内公网运行载体，替代Vercel、承载未来所有Docker服务，核心用途：
 
1. 托管前端H5原型（解决国内访问，替代Vercel）
​
2. 作为Docker宿主机，跑Redis/MinIO/PostgreSQL/Node后端
​
3. 自建轻量CI/CD，联动GitHub实现代码提交自动部署
​
4. 统一对外暴露公网URL，国内用户稳定访问
 
### 二、第一步：服务器初始化（10分钟完成，核心前置）
 
1. 系统与基础环境
 
Lighthouse默认推荐Ubuntu 22.04，重装系统选这个，操作最简单；
 
2. 一键安装核心依赖
 
SSH连接服务器后，执行命令安装必备工具：
 
 
 
3. 安全组放行（关键！否则公网访问不了）
 
腾讯云控制台→Lighthouse→防火墙，放行端口：
 
- 80（HTTP）、443（HTTPS）：前端/后端主访问端口
​
- 22（SSH）：远程连接
​
- 可选：3000（Node后端）、6379（Redis）、5432（PostgreSQL）、9000（MinIO）
 
### 三、当前阶段：无后端，仅前端原型（替代Vercel，国内公网访问）
 
你的前端代码在GitHub，目标：GitHub代码→自动同步到服务器→Nginx托管静态页→生成国内公网H5 URL
 
方案：GitHub+服务器Git+Nginx（最简，零第三方平台）
 
1. 服务器创建前端目录
 
 
 
2. 克隆你的GitHub前端仓库
 
 
 
3. 配置Nginx托管静态页
创建Nginx配置文件： sudo nano /etc/nginx/sites-available/frontend 
写入配置：
 
 
 
4. 启用配置并重启Nginx
 
 
 
5. 实现GitHub提交自动更新（免手动拉代码）
创建定时脚本： nano /var/www/frontend/pull.sh 
 
 
 
赋予权限： chmod +x /var/www/frontend/pull.sh 
设置定时任务（每5分钟拉一次代码）：
 
 
 
✅ 效果：现在访问你的服务器公网IP，就能打开H5，国内秒开，替代Vercel
 
### 四、未来阶段：接入后端，Docker Compose一把梭哈（全栈部署）
 
你规划的Redis+MinIO+PostgreSQL+Node+第三方API，全部塞进 docker-compose.yml ，在Lighthouse上一键启动
 
1. 项目目录规划（服务器统一管理）
 
 
 
2. 极简docker-compose.yml模板（直接复制可用）
 
 
 
3. 一键启动所有服务
 
 
 
4. Nginx反向代理（统一80/443端口访问）
 
把前端、后端API都通过Nginx代理，不用记多端口，直接用IP/域名访问：
 
- 前端： /  指向前端容器
​
- 后端API： /api  指向Node后端3000端口
 
### 五、进阶：联动GitHub实现CI/CD（提交代码自动部署）
 
不用GitLab，直接用GitHub Webhook+服务器脚本，实现：
GitHub提交代码→触发Webhook→服务器自动拉代码、重启Docker服务
 
1. 服务器创建部署脚本，拉取最新代码并重启容器
​
2. GitHub仓库设置Webhook，填写服务器公网接口
​
3. 服务器配置Nginx接收Webhook请求，执行脚本
 
### 六、域名与HTTPS（可选，生产必备）
 
1. 备案域名：腾讯云完成域名备案，绑定服务器
​
2. 免费SSL证书：用Certbot给Nginx配置HTTPS，实现https访问

***

### 一、外包项目(需要与管理/甲方对接)项目开发流水线（按执行顺序）
 
1. Idea 阶段（Layer 1 需求层）
需求分析访谈记录，完成项目初始想法挖掘与用户需求收集。
​
2. Plan + MVP 阶段（Layer 1 规划层）
输出项目计划粗略方案，同步完成最小可行性产品验证调研，明确核心业务范围。（本阶段专注于调研技术可行性和甲方进行交互和专业常识扫盲，安抚甲方情绪，明确sprint开发周期（一般1-2周为一个周期））
​
3. UI/UX 原型阶段（Layer 2 设计层）
完成前端界面设计、交互设计，输出产品原型并开展内部功能测试。
​
4. GitHub + Vercel 模拟上线（Layer 2 研发层）
基于 GitHub 做版本管理、静态版本管控，通过 Vercel 实现内部演示、模拟上线验证。
​
5. PRD 产品文档[^1]（Layer 1 需求功能约束层）
编写产品需求文档，为项目构建唯一约束与开发指导，明确定位/目标/核心规则、需求描述{需求描述}、功能做/不做边界（背景、目标、范围）；用户故事（至少2个场景）； 功能结构（模块拆解）；业务流程（用Mermaid流程图）；交互设计（界面布局 + 交互规则）；数据设计（实体定义）；验收标准（可量化、可测试）。
​
6. Docker‑Compose 一键拉起（Layer 2 后端集成层）
编写  docker‑compose.yml ，整合 API 服务、后端数据库、Redis、MinIO、Nginx、前端分发，完成后端服务集成。
​
7. 测试 + CI/CD 自动化（Layer 3 测试交付层）
接入腾讯云、Lighthouse 完成质量测试；打通 GitHub、Docker Hub 链路，配置 CI/CD 自动化流程；通过 Nginx 解决国内公网访问、代理转发问题。
​
8. 生产环境正式上线（Layer 3 部署上线层）
完成域名注册、ICP 备案、域名绑定，开展合规性校验、线上测试，最终完成产品上线交付。
 
### 二、映射软件工程软件生命周期模型
 
该流程属于敏捷开发 + DevOps 融合的产品全生命周期，完整覆盖：
需求分析 → 产品规划 → 设计原型 → 研发实现 → 版本管控 → 集成部署 → 自动化测试 → 生产上线
同时区分三层核心工作域：
 
- Layer 1：产品/需求层（Idea、Plan+MVP、PRD）
​
- Layer 2：研发/集成层（UI/UX、GitHub+Vercel、Docker集成、CI/CD测试）
​
- Layer 3：上线/运维层（生产环境合规上线交付）


---

[^1]: 例子：项目背景:
- 产品定位: 纯工具、无废话的真题备考引擎
- 目标用户: 考研英语考生
- 核心规则: 每日新词≤50、单词展示3-10次、真题来源

需求描述:
{需求描述}

请按照以下结构输出:
1. 功能概述（背景、目标、范围）
2. 用户故事（至少2个场景）
3. 功能结构（模块拆解）
4. 业务流程（用Mermaid流程图）
5. 交互设计（界面布局 + 交互规则）
6. 数据设计（实体定义）
7. 验收标准（可量化、可测试）

输出格式要求:
- 使用Markdown格式
- 图表使用Mermaid语法
- 所有验收标准必须可量化
- 符合MVP原则（只做最核心功能）

