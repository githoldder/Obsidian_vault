**实验二、白盒测试——基本路径覆盖测试方法**

一、实验目的

通过基本路径覆盖的方法，依据被测程序的逻辑结构设计测试用例，驱动被测程序运行完成的测试，通过本实验达到以下目的：

1、掌握几种常用的逻辑覆盖测试的设计方法；

2、掌握逻辑覆盖测试的具体操作。

二、实验内容

有一段Java程序，判某一年是否是闰年。程序如下：

import java.util.Scanner;

public class testing2 {

  public static void main(String[] args) {

    Scanner reader = new Scanner(System._in_);

    System._out_.print("Enter a year: ");

    int year = reader.nextInt();

    boolean leap = false;

    if (year % 4 == 0) {

      if (year % 100 == 0) {

        if (year % 400 == 0)

          leap = true;

        else

          leap = false;

      }

      else

        leap = true;

    }   

    else

      leap = false;

    if (leap)

      System._out_.println(year + " is a leap year.");

    else

      System._out_.println(year + " is not a leap year.");

  }

} 

注: 以变量leap代表是否是闰年的信息。若是闰年，则leap=1,反之，leap=0。

普通闰年：公历年份是4的倍数，且不是100的倍数的，为闰年（如2004年、2020年等就是闰年）。

世纪闰年：公历年份是整百数的，必须是400的倍数才是闰年（如1900年不是闰年，2000年是闰年）。
①import java.util.Scanner; public class testing2 { public static void main(String[] args) { Scanner reader = new Scanner(System.in); 
System.out.print("Enter a year: "); int year = reader.nextInt(); boolean leap = false; 
② if (year % 4 == 0) { 
③ if (year % 100 == 0) { 
④ if (year % 400 == 0) 
⑤ leap = true; else 
⑥ leap = false; } 
⑤ else leap = true; } 
⑥ else leap = false; 
⑦if (leap) 
System.out.println(year + " is a leap year."); 
else System.out.println(year + " is not a leap year."); } }
图1. 代码语句编号

三、实验要求

1.   根据代码画出程序的**流程图**和**控制流图**（代码语句编号见图1）；

2.   根据控制流图计算圈复杂度，确定基本路径集；

3.   给出每个基本路径的测试用例和预期输出；

4.   按设计的测试用例进行测试，分析每个测试用例的测试情况。

5.      写出测试分析和总结报告。

四、评分标准

1、画出流程图和控制流图，30分

2、给出正确的测试用例，30分

3、测试步骤完整、实验分析正确、总结报告认真。40分

**实验报告部分**

**一、实验准备：**

**（实验预习、测试环境、实验设计）**

**二、实验过程与结果：**

**三、实验小结与体会：**

**（从实验准备、操作、运行结果和遇到问题、解决办法等方面小结。）**

