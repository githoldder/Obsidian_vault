# 004-树与二叉树

> 西蒙学习法·输入阶段 | 408考纲：树的基本概念、二叉树、线索二叉树、树和森林

---

## 一、二叉树基础

### 基本性质（408必考）

1. **第i层最多有 $2^{i-1}$ 个结点**（i≥1）
2. **深度为k的二叉树最多有 $2^k - 1$ 个结点**
3. **叶子结点数 = 度为2的结点数 + 1**（$n_0 = n_2 + 1$）

### 特殊二叉树

| 类型 | 定义 | 特点 |
|------|------|------|
| **满二叉树** | 每层结点数都达到最大 | 高度h，结点数$2^h-1$ |
| **完全二叉树** | 只有最后一层不满，且集中在左侧 | 适合数组存储 |
| **二叉搜索树BST** | 左<根<右 | 中序遍历有序 |
| **平衡二叉树AVL** | 左右子树高度差≤1 | 查找O(logn) |

---

## 二、二叉树遍历（必须能手写代码）

### 递归遍历（简单）

```cpp
// 前序遍历：根-左-右
void preorder(TreeNode* root) {
    if (!root) return;
    cout << root->val << " ";
    preorder(root->left);
    preorder(root->right);
}

// 中序遍历：左-根-右
void inorder(TreeNode* root) {
    if (!root) return;
    inorder(root->left);
    cout << root->val << " ";
    inorder(root->right);
}

// 后序遍历：左-右-根
void postorder(TreeNode* root) {
    if (!root) return;
    postorder(root->left);
    postorder(root->right);
    cout << root->val << " ";
}
```

### 非递归遍历（408重点）

```cpp
// 前序遍历（非递归）
vector<int> preorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> st;
    if (root) st.push(root);
    while (!st.empty()) {
        TreeNode* node = st.top(); st.pop();
        res.push_back(node->val);
        if (node->right) st.push(node->right);  // 先右后左
        if (node->left) st.push(node->left);
    }
    return res;
}

// 中序遍历（非递归）
vector<int> inorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> st;
    TreeNode* cur = root;
    while (cur || !st.empty()) {
        while (cur) {  // 一路向左
            st.push(cur);
            cur = cur->left;
        }
        cur = st.top(); st.pop();
        res.push_back(cur->val);
        cur = cur->right;
    }
    return res;
}

// 后序遍历（非递归）- 用前序变形
vector<int> postorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> st;
    if (root) st.push(root);
    while (!st.empty()) {
        TreeNode* node = st.top(); st.pop();
        res.push_back(node->val);
        if (node->left) st.push(node->left);
        if (node->right) st.push(node->right);
    }
    reverse(res.begin(), res.end());  // 根-右-左 反转 → 左-右-根
    return res;
}
```

### 层次遍历（BFS）

```cpp
vector<vector<int>> levelOrder(TreeNode* root) {
    vector<vector<int>> res;
    if (!root) return res;
    queue<TreeNode*> q;
    q.push(root);
    while (!q.empty()) {
        int size = q.size();
        vector<int> level;
        for (int i = 0; i < size; i++) {
            TreeNode* node = q.front(); q.pop();
            level.push_back(node->val);
            if (node->left) q.push(node->left);
            if (node->right) q.push(node->right);
        }
        res.push_back(level);
    }
    return res;
}
```

---

## 三、BST二叉搜索树

### 基本操作

```cpp
// 查找
TreeNode* searchBST(TreeNode* root, int val) {
    while (root && root->val != val) {
        root = val < root->val ? root->left : root->right;
    }
    return root;
}

// 插入
TreeNode* insertIntoBST(TreeNode* root, int val) {
    if (!root) return new TreeNode(val);
    if (val < root->val) root->left = insertIntoBST(root->left, val);
    else root->right = insertIntoBST(root->right, val);
    return root;
}

// 删除（最复杂）
TreeNode* deleteNode(TreeNode* root, int key) {
    if (!root) return nullptr;
    if (key < root->val) root->left = deleteNode(root->left, key);
    else if (key > root->val) root->right = deleteNode(root->right, key);
    else {
        if (!root->left) return root->right;
        if (!root->right) return root->left;
        // 找右子树最小值替代
        TreeNode* minNode = root->right;
        while (minNode->left) minNode = minNode->left;
        root->val = minNode->val;
        root->right = deleteNode(root->right, minNode->val);
    }
    return root;
}
```

**删除的三种情况**：
1. 叶子结点：直接删除
2. 只有一个孩子：用孩子替代
3. 有两个孩子：用右子树最小值（或左子树最大值）替代，然后删除那个值

---

## 四、线索二叉树（408概念）

**核心思想**：利用空指针域存储前驱/后继，实现非递归遍历

| 类型 | 线索指向 |
|------|---------|
| 中序线索 | 左指针指前驱，右指针指后继 |
| 前序线索 | 左指针指前驱（或孩子），右指针指后继（或孩子） |
| 后序线索 | 类似 |

**考点**：给定遍历序列，画出线索二叉树

---

## 五、并查集（竞赛重点）

```cpp
class UnionFind {
    vector<int> parent, rank_;
public:
    UnionFind(int n) {
        parent.resize(n);
        rank_.resize(n);
        for (int i = 0; i < n; i++) parent[i] = i;
    }
    
    int find(int x) {
        if (parent[x] != x) parent[x] = find(parent[x]);  // 路径压缩
        return parent[x];
    }
    
    bool unite(int x, int y) {
        int px = find(x), py = find(y);
        if (px == py) return false;
        if (rank_[px] < rank_[py]) swap(px, py);
        parent[py] = px;
        if (rank_[px] == rank_[py]) rank_[px]++;
        return true;
    }
    
    bool connected(int x, int y) { return find(x) == find(y); }
};
```

---

## 六、费曼自测

1. 证明：二叉树叶子结点数 = 度为2的结点数 + 1
2. 中序遍历的非递归算法中，什么时候访问当前结点？
3. BST删除有两个孩子的结点时，为什么可以用右子树最小值替代？
4. 并查集的路径压缩是什么？时间复杂度是多少？

---

## 七、本周刷题清单

### 基础（408必会）

| 题号 | 题目 | 知识点 |
|------|------|--------|
| LeetCode 144 | Binary Tree Preorder Traversal | 前序遍历 |
| LeetCode 94 | Binary Tree Inorder Traversal | 中序遍历 |
| LeetCode 145 | Binary Tree Postorder Traversal | 后序遍历 |
| LeetCode 102 | Binary Tree Level Order Traversal | 层次遍历 |
| LeetCode 104 | Maximum Depth of Binary Tree | 树高 |
| LeetCode 101 | Symmetric Tree | 对称二叉树 |
| LeetCode 700 | Search in a Binary Search Tree | BST查找 |
| LeetCode 701 | Insert into a Binary Search Tree | BST插入 |
| LeetCode 450 | Delete Node in a BST | BST删除 |

### 进阶（竞赛）

| 题号 | 题目 | 知识点 |
|------|------|--------|
| LeetCode 105 | Construct Binary Tree from Preorder and Inorder | 前序+中序建树 |
| LeetCode 106 | Construct Binary Tree from Inorder and Postorder | 中序+后序建树 |
| LeetCode 236 | Lowest Common Ancestor of a Binary Tree | LCA |
| LeetCode 124 | Binary Tree Maximum Path Sum | 树形DP |
| LeetCode 684 | Redundant Connection | 并查集 |
| LeetCode 547 | Number of Provinces | 并查集 |

---

tags: [树, 二叉树, BST, 并查集, 408, 西蒙学习法]
date: 2026-04-10
