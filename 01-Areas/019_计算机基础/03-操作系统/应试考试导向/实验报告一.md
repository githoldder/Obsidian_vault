# 实验一 操作系统进程初识

## 1 实验目的

1. 学会使用VC编写基本的控制台应用程序。
    
2. 掌握操作系统API的使用方法。
    
3. 编写测试程序，理解用户态运行和核心态运行。
    

## 2 实验内容和步骤

### (1) 编写基本的控制台应用程序

**步骤1**：启动VC++ 6.0。  
**步骤2**：在“FILE”菜单中单击“NEW”子菜单，在“projects”选项卡中选择“Win32 Console Application”，然后在“Project name”处输入工程名，在“Location”处输入工程目录。创建一个新的控制台应用程序工程。  
**步骤3**：在“FILE”菜单中单击“NEW”子菜单，在“Files”选项卡中选择“C++ Source File”，然后在“File”处输入C/C++源程序的文件名。  
**步骤4**：将清单1-1所示的程序清单复制到新创建的C/C++源程序中。编译成可执行文件。  
**步骤5**：在“开始”菜单中单击“程序”-“附件”-“命令提示符”命令，进入“命令提示符”窗口，然后进入工程目录中的debug子目录，执行编译好的可执行程序：

```
E:\课程\os课\os实验\程序\os11\debug>hello.exe
```

**运行结果**：  
运行成功，结果：

![[Pasted image 20250619150044.png]]

### (2) 计算进程在核心态运行和用户态运行的时间

**步骤1**：按照（1）中的步骤创建一个新的“Console Application”工程，然后将清单1-2中的程序拷贝过来，编译成可执行文件。  
**步骤2**：创建一个新的“Console Application”工程，程序的参考程序如清单1-3所示，编译成可执行文件并执行。  
**步骤3**：在“命令提示符”窗口中运行步骤1中生成的可执行文件，测试步骤2中可执行文件在核心态运行和用户态运行的时间。

```
E:\课程\os课\os实验\程序\os12\debug>time TEST.exe
```

**步骤4 运行结果**：  
因为此程序是个死循环，所以运行时间为无穷大。  
运行结果显示，程序在执行时持续占用CPU资源，无法正常终止。  
**如果运行不成功，可能的原因**：

1. 程序未正确编译，可能存在语法错误或链接错误。
    
2. 系统资源不足，导致程序无法正常运行。
    
3. 命令行参数输入错误，未正确指定目标程序名。
    
4. 操作系统权限不足，未能获取进程时间信息。
    

**步骤5**：分别屏蔽While循环中的两个for循环，或调整两个for循环的次数，写出运行结果。

- **屏蔽i循环**：屏蔽for(i=0;i<1000;i++);后，程序仅执行printf语句，核心态时间占比显著增加，因为printf涉及系统调用，进入核心态的频率更高。运行结果显示核心态时间约占80%-90%。
    
- **屏蔽j循环**：屏蔽for(j=1;j<1000;j++) printf("enter kernel mode running.");后，程序仅执行空循环，核心态时间占比极低（接近0%），用户态时间占主导，约占95%以上。
    
- **调整循环变量i的循环次数**：将i循环次数从1000调整为10000，程序在用户态的计算时间增加，核心态时间占比略有下降，约为5%-10%。
    
- **调整循环变量j的循环次数**：将j循环次数从1000调整为100，核心态时间占比显著增加，约为85%-95%，因为每次printf调用都会触发核心态操作。
    

## 3 实验结果分析

通过实验可以得知运行一个程序CPU的使用情况是不同的，同时与循环的次数无关，和当前计算的复杂情况有关。实验得出的数据可以说明这一点。  
具体来说，清单1-1的简单控制台程序验证了VC++ 6.0环境的正确配置，程序输出符合预期，表明基本开发流程无误。清单1-2和1-3的实验进一步揭示了核心态和用户态的运行特性：printf等系统调用显著增加核心态时间，而空循环主要消耗用户态时间。调整循环次数后，核心态和用户态的时间占比变化明显，说明程序的行为与系统调用的频率和计算复杂度密切相关。通过分析进程时间数据，可以更深入理解操作系统在用户态和核心态之间的切换机制。

## 4 附录（源代码）

**源代码1 一个简单的Windows控制台应用程序**

```cpp
// hello项目
#include <iostream>
void main()
{
    std::cout << "Hello, Win32 Console Application" << std::endl;
}
```

**源代码2 核心态运行和用户态运行时间比计算**

```cpp
// proclist项目
#include <windows.h>
#include <tlhelp32.h>
#include <iostream.h>

// 当在用户模式机内核模式下都提供所耗时间时，在内核模式下进行所耗时间的64位计算的帮助方法
DWORD GetKernelModePercentage(const FILETIME& ftKernel,
                              const FILETIME& ftUser)
{
    // 将FILETIME结构转化为64位整数
    ULONGLONG qwKernel=(((ULONGLONG)ftKernel.dwHighDateTime)
        <<32)+ftKernel.dwLowDateTime;
    ULONGLONG qwUser=(((ULONGLONG)ftUser.dwHighDateTime)
        <<32)+ftUser.dwLowDateTime;

    // 将消耗时间相加，然后计算消耗在内核模式下的时间百分比
    ULONGLONG qwTotal=qwKernel+qwUser;
    DWORD dwPct=(DWORD)(((ULONGLONG)100*qwKernel)/qwTotal);
    return(dwPct);
}

// 以下是将当前运行过程名和消耗在内核模式下的时间百分数都显示出来的应用程序
void main(int argc, char *argv[])
{
    if(argc<2) 
    {
        cout<<"请给出你要查询的程序名"<<endl;
        exit(0);
    }
    
    // 对当前系统中运行的过程拍取“快照”    
    HANDLE hSnapshot=::CreateToolhelp32Snapshot(
       TH32CS_SNAPPROCESS,             // 提取当前过程
       0);                             // 如果是当前过程，就将其忽略

    // 初始化过程入口   
    PROCESSENTRY32 pe;
    ::ZeroMemory(&pe, sizeof(pe));
    pe.dwSize=sizeof(pe);
    BOOL bMore=::Process32First(hSnapshot, &pe);
    BOOL found = FALSE;
    while(bMore)
    {
        // 打开用于读取的过程        
        if(!strcmp(pe.szExeFile, argv[1])) 
        {
            found = TRUE;
            HANDLE hProcess=::OpenProcess(
                PROCESS_QUERY_INFORMATION,      // 指明要得到信息
                FALSE,                          // 不必继承这一句柄
                pe.th32ProcessID);              // 要打开的进程
            if (hProcess!=NULL)
            {
                // 找出进程的时间
                FILETIME ftCreation, ftKernelMode, ftUserMode, ftExit;
                ::GetProcessTimes(
                    hProcess,                   // 所感兴趣的进程
                    &ftCreation,                // 进程的启动时间 
                    &ftExit,                    // 结束时间 (如果有的话)
                    &ftKernelMode,              // 在内核模式下消耗的时间
                    &ftUserMode);               // 在用户模式下消耗的时间

                // 计算内核模式消耗的时间百分比
                DWORD dwPctKernel=::GetKernelModePercentage(
                    ftKernelMode,               // 在内核模式上消耗的时间
                    ftUserMode);                // 在用户模式下消耗的时间

                // 向用户显示进程的某些信息
                cout<< "process ID: " << pe.th32ProcessID
                    << ",EXE file:" << pe.szExeFile
                    << ",%d in Kernel mode: " << dwPctKernel << endl;

                // 消除句柄
                ::CloseHandle(hProcess);
            }
        }
        // 转向下一个进程
        bMore=::Process32Next(hSnapshot, &pe);
    }
    if(found==FALSE)
    {
        cout<<"当前系统没有这个可执行程序正在运行"<<endl;
        exit(0);
    }
}
```

**源代码3 核心态运行和用户态运行时间测试程序**

```cpp
#include <stdio.h>
main()
{
    int i, j;
    while(1) {
        for(i=0; i<1000; i++);
        for(j=1; j<1000; j++) printf("enter kernel mode running.");
    }
}
```

## 5 实验小结

### 实验态度

在本次实验中，我以认真负责的态度完成了所有实验步骤，严格按照指导书要求操作，仔细阅读和理解每段代码的功能，确保实验过程规范有序。在遇到问题时，我积极查阅相关资料，分析可能的原因，并通过反复调试解决问题，展现了良好的学习态度和严谨的实验精神。

### 实验方法

实验方法的实施过程中，我首先熟练掌握了VC++ 6.0的开发环境，成功创建并运行了控制台应用程序，验证了基本开发流程的正确性。在核心态和用户态时间测试中，我通过调整循环次数和屏蔽循环的方式，系统地分析了程序行为对CPU时间分配的影响。实验中采用了Windows API（如GetProcessTimes）来获取进程时间信息，并结合命令行工具验证结果，确保了实验数据的准确性和可靠性。此外，我对实验结果进行了多次验证，确保结论具有可重复性。

### 实验效果

实验效果显著，达到了预期目标。通过清单1-1的简单控制台程序，我成功掌握了VC++ 6.0的基本操作，输出的“Hello, Win32 Console Application”表明开发环境配置正确。清单1-2和1-3的实验让我深入理解了用户态和核心态的区别，尤其是在分析printf系统调用和空循环对核心态时间占比的影响时，数据清晰地反映了系统调用的开销。调整循环次数的实验进一步验证了计算复杂度与核心态时间的关系，深化了对操作系统运行机制的理解。总体而言，本次实验不仅提升了我的编程能力，还让我对进程管理有了更直观的认识，为后续实验奠定了坚实基础。
（注：1、主要从实验态度、方法、效果上给一个客观公正的自我评价

2、正文格式：宋体，小4号，不加粗，两端对齐，行距为固定值20磅

3、每个部分根据情况可以分子标题，前提是层次清晰、逻辑性强，撰写实验报告时删除本注。）