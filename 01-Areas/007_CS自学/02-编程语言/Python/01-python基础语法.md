---
tags:
  - python
  - 编程
---

> [!NOTE] 原语
> 在python中primitive是最小的可求值单元，也就是字面量

python有很多人性化输出

python中用双引号‘’和单引号“”引起来的就是字符串

unicode是万国字符码

number 是 ==数==
在python中会有特殊颜色代码自动标注

> [!NOTE] 组合
> 将原语组合起来，可以做一些简单的运算

算术运算符
**加减乘除**和**取模运算**
逻辑运算符and、or、not用于布尔值的组合，成员运算符in可以测试一个值是否包含在容器中。

使用运算符会比调API要快一点，因为原生的支持更好

numpy中的矩阵运算（实际上可以写成一个函数）


> [!NOTE] 命名与对象绑定
> 命名：变量赋值name =  实际上是把“名字”绑定到”对象“。

```python
a=10
b=a
a,b,id(a),id(b)
```

本质上a和b都是指向在了10所在的内存地址块上

“对象“其实可以指代程序中的任何数据，例如变量、数组、列表、元组、字典、集合

可变性

数值类型和精度


> [!NOTE] 真值与短路
> 短路的特性可以用于在用户输入为空值时使用默认值
return a or default
or 的判定规则是，两边都为false才为false，左边若为空，也就是false，那就会执行到右边的默认值

echo

表达式

输入和输出
input()函数总是返回字符串


分支语句：if/elif/else
`:`和缩进几乎同时出现，只有涉及到语句的时候才会使用缩进[^1]

字符串的不可变性

列表的动态特性
列表是可变的有序序列，是python中最常用的数据结构之一，列表支持索引、切片以及各种操作

元组和拆包

字典与键值映射

从列表来生成字典

推导式

切片和步长

格式化输出的现代语法
f-string

python库numpy
pandas是基于numpy的基础往上开发

对于数据应该怎么处理
使用工具
原始数据->读取数据->结构化转化（通常是二维表格）->质量检查（评估）->过滤/聚合（多个数据聚合到一个点，比如求平均值）->变形（缩放）->记录数据

行 列 帧 系列
row column  
series（一维带索引）
DataFrame（行索引+列名）

lambda 本质上是定义了一个匿名函数

可以用多个列名去取每一行的数据来表示二维表格

布尔过滤

标准化->修补->去重->追踪
去重策略-异常检测-流水线化

---
数据清洗与集成
不方便收集、数据缺失->缺失->引入外部领域规律、假设、算法
缺失策略[^2]
- 删除：当table表格内的数据离散化程度过高时，通过删除某行/列缺失过多的数据（当然要给出阈值和证据）
- 众数：GMM（高斯混合模型）->KNN（K近邻算法）->EM（期望最大化算法）
- 标记：缺失也是信息
冗余：重复
异常检测：错误->单变量粗筛
多变量粗筛
噪声缓和
pathlib
单元小项目-数据清洗
正则表达式可以对数据进行预处理
table（表格）->
|
form（表单）->高级数据验证

[^1]: 一开始没有语句，只有优雅的表达式
	但是由于简单好用的特性，使得python的用户一下子多了起来
	python的开创者有偶像包袱，为了迎合市场，将python打造成国民级语言
	才加入了语句，后来语法越加越多，渐渐变成了石山代码

[^2]: 数据科学这门科目是统计学和计算机科学的交叉学科，所以会出现很多统计学中的专业名词
