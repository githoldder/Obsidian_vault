#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>
#include <set>
#include <unordered_map>
#include <unordered_set>
#include <tuple>
#include <array>
using namespace std;

// 快速 IO 模板 + 常用别名
// 使用方法：把此模板复制到新题目文件开头

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 示例：读写
    // int n; if (cin >> n) { /* ... */ }

    return 0;
}
