# 002-线性表与双指针技巧

> 西蒙学习法·输入阶段 | 408考纲：线性表的基本操作、顺序表、链表

---

## 一、顺序表（数组）

### 核心操作复杂度

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 随机访问 | O(1) | 通过下标直接访问 |
| 尾部插入/删除 | O(1) | 不涉及元素移动 |
| 中间插入/删除 | O(n) | 需要移动后续元素 |
| 查找 | O(n) | 无序时线性查找 |

### 408考点

1. **插入算法**：从后向前移动元素，腾出位置
2. **删除算法**：从前向后移动元素，覆盖被删元素
3. **有序表合并**：双指针法，O(n+m)

---

## 二、链表

### 单链表核心操作

```cpp
struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(nullptr) {}
};

// 头插法创建链表
ListNode* create(vector<int>& nums) {
    ListNode* dummy = new ListNode(0);
    for (int x : nums) {
        ListNode* node = new ListNode(x);
        node->next = dummy->next;
        dummy->next = node;
    }
    return dummy->next;
}

// 反转链表（迭代）
ListNode* reverse(ListNode* head) {
    ListNode *prev = nullptr, *curr = head;
    while (curr) {
        ListNode* next = curr->next;
        curr->next = prev;
        prev = curr;
        curr = next;
    }
    return prev;
}

// 反转链表（递归）
ListNode* reverseRecursive(ListNode* head) {
    if (!head || !head->next) return head;
    ListNode* newHead = reverseRecursive(head->next);
    head->next->next = head;
    head->next = nullptr;
    return newHead;
}
```

### 408必会代码

1. **单链表反转**（递归+非递归）
2. **链表中点**（快慢指针）
3. **链表环检测**（Floyd判圈）
4. **合并两个有序链表**

---

## 三、双指针技巧

### 3.1 对撞指针（两端向中间）

**适用场景**：有序数组、两数之和

```cpp
// 两数之和（有序数组）
vector<int> twoSum(vector<int>& nums, int target) {
    int left = 0, right = nums.size() - 1;
    while (left < right) {
        int sum = nums[left] + nums[right];
        if (sum == target) return {left, right};
        else if (sum < target) left++;
        else right--;
    }
    return {};
}
```

### 3.2 快慢指针（同向移动）

**适用场景**：链表问题、原地修改数组

```cpp
// 删除有序数组重复项（原地修改）
int removeDuplicates(vector<int>& nums) {
    if (nums.empty()) return 0;
    int slow = 0;
    for (int fast = 1; fast < nums.size(); fast++) {
        if (nums[fast] != nums[slow]) {
            slow++;
            nums[slow] = nums[fast];
        }
    }
    return slow + 1;
}

// 链表中点（快慢指针）
ListNode* middleNode(ListNode* head) {
    ListNode *slow = head, *fast = head;
    while (fast && fast->next) {
        slow = slow->next;
        fast = fast->next->next;
    }
    return slow;
}
```

### 3.3 滑动窗口（动态区间）

**适用场景**：子串问题、连续子数组

```cpp
// 无重复字符的最长子串
int lengthOfLongestSubstring(string s) {
    unordered_map<char, int> window;
    int left = 0, res = 0;
    for (int right = 0; right < s.size(); right++) {
        char c = s[right];
        window[c]++;
        while (window[c] > 1) {  // 不满足条件，收缩左边界
            window[s[left]]--;
            left++;
        }
        res = max(res, right - left + 1);
    }
    return res;
}
```

---

## 四、408 vs 竞赛差异

| 维度 | 408考研 | 竞赛/机试 |
|------|---------|-----------|
| 重点 | 概念、算法描述、手算 | 代码实现、边界处理 |
| 链表 | 画图分析、伪代码 | 完整可运行代码 |
| 双指针 | 理解思想 | 熟练套用模板 |

**建议**：408阶段理解原理+能手写伪代码，机试阶段必须能敲出完整代码。

---

## 五、费曼自测

1. 顺序表和链表的核心区别是什么？分别适合什么场景？
2. 链表反转的递归思路是什么？
3. 快慢指针找链表中点时，快指针的终止条件是什么？
4. 滑动窗口的模板框架是什么？

---

## 六、本周刷题清单

### 基础（408必会）

| 题号 | 题目 | 知识点 |
|------|------|--------|
| LeetCode 206 | Reverse Linked List | 链表反转 |
| LeetCode 21 | Merge Two Sorted Lists | 链表合并 |
| LeetCode 141 | Linked List Cycle | 环检测 |
| LeetCode 142 | Linked List Cycle II | 环入口 |
| LeetCode 19 | Remove Nth Node From End | 双指针 |

### 进阶（竞赛）

| 题号 | 题目 | 知识点 |
|------|------|--------|
| LeetCode 3 | Longest Substring Without Repeating Characters | 滑动窗口 |
| LeetCode 15 | 3Sum | 对撞指针 |
| LeetCode 11 | Container With Most Water | 对撞指针 |
| LeetCode 26 | Remove Duplicates | 快慢指针 |
| LeetCode 283 | Move Zeroes | 快慢指针 |

---

tags: [线性表, 链表, 双指针, 408, 西蒙学习法]
date: 2026-04-10
